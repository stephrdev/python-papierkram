Installation
============

* Install with pip::

    pip install python-papierkram


* Your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # ...
        'papierkram',
    )


TODO: Add additional installation steps.
